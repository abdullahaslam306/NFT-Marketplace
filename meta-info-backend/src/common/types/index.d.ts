export type Country = {
  iso3: string,
  name: string
}

export type State = {
  code: string,
  name: string
}

export type Currency = {
  name: string
  code: string
  symbol: string
}

export type Language = {
  name: string
  code: string,
}